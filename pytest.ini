[pytest]
# Caminho dos testes
testpaths = tests
# Verbosidade padrão
addopts = -v --tb=short --timeout=120

# Marcadores registrados
markers =
    llm: testes que requerem LLM carregado (GPU). Use -m "not llm" para pular.

# Output mais limpo
log_cli = false